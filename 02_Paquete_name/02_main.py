import module_name

#Cuando un módulo es importado, su contenido se ejecuta implícitamente por Python.
# En este caso, como module.py ya tiene contenido, se imprime directamente en consola al ejecutar este import :)